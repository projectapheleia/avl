# Copyright 2024 Apheleia
#
# Signals built and driven per clock edge. Everything else this benchmark needs
# is shared by the group - see ../create_and_send.conf.

BENCH_SIGNALS := 8

include $(dir $(BENCH_DIR))create_and_send.conf
