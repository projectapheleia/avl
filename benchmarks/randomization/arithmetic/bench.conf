# Copyright 2024 Apheleia
#
# Per benchmark defaults - see ../../common.mk for the meaning of each.
#
# Sums and differences over 16 bit vectors are the dearest category to solve, so
# fewer randomizations are run to keep the benchmark to a sensible length.

ITERATIONS ?= 200
