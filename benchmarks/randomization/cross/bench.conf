# Copyright 2024 Apheleia
#
# Per benchmark defaults - see ../../common.mk for the meaning of each.
#
# Every field depends on every other, so each solve is dear. Fewer
# randomizations keep the benchmark to a sensible length.

ITERATIONS ?= 200
