# Copyright 2024 Apheleia
#
# Per benchmark defaults - see ../../common.mk for the meaning of each.
#
# A single $urandom is far cheaper than advancing the clock, so this benchmark
# draws a burst of them per edge, and enough of them in total to measure.

ITERATIONS ?= 1000000
BURST      ?= 1000
