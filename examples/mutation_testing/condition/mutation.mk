# Six branch decisions, one per comparison operator, three of them written
# as if statements and three as conditional expressions. Each yields three
# mutations - held open, held shut, and its comparison nudged - so every
# combination the condition class can make is on show.
MUTATION_TYPES := condition
MUTATION_COUNT := 18
